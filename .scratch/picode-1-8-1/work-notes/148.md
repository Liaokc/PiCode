# 148 — pi-mcp-adapter 2.37 fidelity 工作笔记

- 开工：2026-09-25 02:28（执行会话 worker，worktree `.worktrees/wt-148-mcp-fidelity`，分支 `t148-mcp-fidelity`）
- 票面：`.scratch/picode-1-8-1/issues/148-mcp-237-fidelity.md`（ready-for-agent）
- 环境核对：用户级 pi-mcp-adapter = 2.37.0（`~/.pi/agent/npm/node_modules/pi-mcp-adapter/package.json`，只读核对）；用户级 pi-subagents = 0.71.0（只读核对）；本 worktree 捆绑 SDK 0.86.1（146 并行票负责 bump，文件面零重叠）
- 关键证据源：`.scratch/picode-1-8-1/research/pi-mcp-adapter-2.37.0-diff.md`（§2.1 合并、§2.2 快照版本、§2.4 OAuth、§2.5 README:76 未变）

## 步骤

1. 忠实注释：`src/shared/mcp-management.ts:9` / `src/shared/mcp-status.ts:10` 2.35.0 → 2.37.0 + ticket 148 重验注记
   - management：合并规则逐行未变（仅外包 applySettingDefaults，settings.exposeResources 默认——仅显示保真缺口且仅启用时）、jev settings.jev 共存（writeJevSemanticSearchConfig 写 Pi 自有层；合并只读服务器条目、写保留未知键）、OAuth 流未变（getValidToken 修复仅及扩展侧 token 查询 API，/mcp-auth 流逐行未变）
   - status：adapter mcp-status.ts 字节未变、MCP_STATUS_SNAPSHOT_VERSION=1 与频道 `pi-mcp-adapter/status/v1` 未 bump（v1 投影与钉频道订阅继续成立）、deferWithMissingMetadata 诚实降级注记
2. jev 共存单测（Q7-④）：`tests/shared/mcp-management.test.ts` 既有 settings 用例扩展，4 个新用例
   - parse：`settings.jev`（`{semanticSearch:true, allowedServers:[...]}`，writeJevSemanticSearchConfig 写产物形状）parse 保留
   - merge：mergeMcpLayers 只读服务器条目（无幽灵行、序列化无 jev 泄漏）
   - write/remove：deriveServerEntryWrite / deriveServerEntryRemove 写后 settings.jev 原样幸存
   - 顺手 lint 修复（同文件既有问题）：行内 `import()` 类型注解改为顶部 type import（consistent-type-imports）——touched 文件 lint 全绿，零行为变化
3. 验证（见下）

## 验证结果（矩阵）

| 项 | 结果 | 证据 |
|---|---|---|
| vitest 全套 | PASS 2193/2193（125 文件） | `env -u PI_SUBAGENT_CHILD -u PI_SUBAGENTS_HERDR_BRIDGE -u PI_SUBAGENTS_PI_CODING_AGENT_PACKAGE_ROOT npm test`；首跑 1 失败系本 subagent 会话继承 `PI_SUBAGENTS_PI_CODING_AGENT_PACKAGE_ROOT` 触发 subagent-runner-root 幂等守卫用例——env -u 后全绿（env 地雷类，非代码问题） |
| mcp 定向单测 | PASS（mcp-management 56 + mcp-status 全部） | `npx vitest run tests/shared/mcp-management.test.ts tests/shared/mcp-status.test.ts` → 72 passed |
| typecheck | PASS | `npm run typecheck` 零输出 |
| eslint touched | PASS | `npx eslint src/shared/mcp-management.ts src/shared/mcp-status.ts tests/shared/mcp-management.test.ts` → clean（仓库其余 22 个 lint 问题在干净树上同样存在，全部 pre-existing，与本票无关） |
| smoke:host 全套 | PASS（EXIT=0） | `env -u …(同上) npm run smoke:host` → `SMOKE PASS host contract smoke complete`；Round G：`ticket-89 MCP OAuth bridge contract ok — start → live notices → completed(ok=false), no credentials touched`；Round I：`ticket-96 mcp_status ok (version 1, 4 servers, lazy=not-connected, off=disabled)` |
| smoke:electron（含 MCP 段） | PASS（EXIT=0） | `env -u …(同上) npm run smoke:electron` → `SMOKE done` + `multi_shutdown_no_orphans_ok 47 hosts`；MCP 段全绿：`mcp_section_open_ok / mcp_cards_badges_ok / mcp_status_projection_ok / mcp_enable_flag_ok / mcp_disable_flag_ok / mcp_add_global_ok / mcp_edit_global_ok / mcp_remove_ok / mcp_layer_entries_ok / mcp_oauth_autocomplete_ok / mcp_status_live_update_ok / mcp_paste_dialog_ok / mcp_oauth_manual_paste_ok / mcp_external_zero_write_ok / mcp_credentials_zero_leak_ok / mcp_status_lazy_untouched_ok`（OAuth 双腿 = autocomplete + manual paste 均绿） |
| smoke 锁纪律 | 遵守 | mkdir smoke.lock 原子获取（attempt 1）→ ps 自查通道空闲 → 跑完 rmdir 释放；全程未触碰 ~/.pi（adapter/subagents 版本核对为只读） |

### 过程披露

- smoke:host 首跑（未剥 subagent 标记）在 Round K（ticket-101 subagent stop）失败：`the stop request timed out (pi-subagents did not answer)`。根因：本 worker 会话自身是 pi subagent（`PI_SUBAGENT_CHILD` 在 env 中），main 分支 forkHost() 尚未剥该标记（**票 144 的并行修复面**），forked host 内 pi-subagents 拒绝注册 → stop RPC 无人应答。剥标记重跑后全套 PASS（Round G/I 两次均绿）。与本票改动（纯注释 + 纯模型测试）无因果。
- smoke:electron 跑的是全量 app smoke（MCP 段不可单独选择，PICODE_SMOKE_STAGE 仅支持 t134-sanitized/t100-queue），MCP 段全绿即票面要求；electron-smoke 包装器自身已剥两个 child 标记（ticket 142），另剥 `PI_SUBAGENTS_PI_CODING_AGENT_PACKAGE_ROOT` 以对齐干净终端环境。
- 验证组合为 用户级 adapter 2.37.0 × 捆绑 SDK 0.86.1（票面写的 0.87.1 组合属票 146 的 bump 面与本票批次回归；2.37 的 peer 范围含 ^0.86，research §3 ⑤）。
- package-lock.json 的 npm install 附带变更（license 行）已还原——保持与票 146 文件面零重叠。

## 收尾

- 分支 tip：`854a6a1`（`fix(148): pi-mcp-adapter 2.37.0 fidelity — comment watermark + jev coexistence + MCP smoke re-verified`），`git status` 干净
- 不 push、不打 tag、不合并；等评审
- 截图：不适用（纯注释 + 纯模型单测 + smoke 复核，无 UI 可见面变化）
