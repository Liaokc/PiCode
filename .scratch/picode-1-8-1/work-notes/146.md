# 146 — SDK 升级 0.86.1 → 0.87.1（锁版 bump + 版本水位断言 + 全量验证）

- 开工：2026-09-24 02:26（worktree `t146-sdk-bump`，基线 `abfab25`）
- 收尾提交：见文末（分支 tip sha）
- 执行会话：worker（W1 批次）；master 裁决记录见「smoke 阻塞与裁决」节

## 改动面（票面四项，全部完成）

1. `package.json:72`：`"@earendil-works/pi-coding-agent": "0.86.1"` → `"0.87.1"`（精确锁版，无范围）。
2. `npm install` 同步：node_modules 实装 SDK 0.87.1，嵌套全家桶 chord / pi-agent-core / pi-ai / pi-telemetry / pi-tui 全部 0.87.1；package-lock.json diff 仅 earendil 家族版本互换（root spec + 5 嵌套包 + license 行 + 一处 dev 旗标），零非 earendil 变更 → 渲染层 bundle 与 0.86.1 装机逐字节同源，bump 不可能影响纯 UI 行为。
3. `tests/main/bundled-sdk-versions.test.ts`：pin 断言字面量 0.86.1 → 0.87.1（含 it 描述）；头注改写（Ticket 134 re-pinned at 0.87.1 by ticket 146；地板语义注记：守 pi-subagents 0.70.x 旧装，0.71.0 已删硬导入但地板仍诚实）；pi-ai `>= 0.86.1` 地板断言与探针（createReadOnlyTools/convertToLlm、transcript utils）逐字保留。
4. `scripts/package.mjs`：t134 对齐块注释 pinned 0.87.1（代码动态读 package.json，零功能改动；pi-ai ≥0.86.1 地板检查维持原值）。

## 验证矩阵

| 项 | 结果 | 证据 |
|---|---|---|
| vitest 全套 | PASS 125 files / 2189 tests | `env -u PI_SUBAGENT_CHILD -u PI_SUBAGENTS_HERDR_BRIDGE -u PI_SUBAGENTS_PI_CODING_AGENT_PACKAGE_ROOT npm test`（worker 会话第三泄漏变量必须一并 unset——t111 幂等测试读 PI_SUBAGENTS_PI_CODING_AGENT_PACKAGE_ROOT 早退分支；unset 后 7/7 绿） |
| bundled-sdk-versions 五断言 | PASS | pin=0.87.1；installed=pinned（node_modules 实装 0.87.1）；pi-ai 0.87.1 ≥ 0.86.1 地板；createReadOnlyTools/convertToLlm function；transcript utils function |
| typecheck | PASS | `npm run typecheck` exit 0（node + web 两 project） |
| eslint touched | PASS | `npx eslint tests/main/bundled-sdk-versions.test.ts scripts/package.mjs` exit 0 |
| smoke stage 1 build | PASS | 两次全套各 9s |
| smoke stage 2 host contract | PASS ×4（重载×2 + 安静×2） | 78–98s 全绿：真实 SDK 0.87.1 流式/工具/审批/队列/resume/rename/tree/fork/候选态/t80 闸门/t112 pi-0.86 会话兼容（271/271 行无损） |
| smoke stage 3 pty | PASS ×4 | 3–13s |
| smoke stage 4 usage | PASS ×2（安静窗口） | 6s：真实库快照契约 + 重扫幂等 + 增量机制全套；重载窗口下两次失败（见下） |
| smoke stage 5 interop | PASS ×3 | 双向：捆绑 0.87.1 SDK 打开全局 TUI 写的会话（orchestrator 会话 177 entries）；全局 TUI 0.87.1 重开捆绑 0.87.1 host 会话（marker 5 entries 回读）+ usage fold + resume |
| smoke stage 6 electron | 阻塞（环境性 flake，见下） | 五次尝试失败点各不相同：empty-state host boot session_error ×2（重载）→ t105 PTY keystroke echo（重载）→ t123 dead-group drag（安静#1，190 断言后）→ t136 panel drag（安静#2，400 断言后，含 t90/99/101 全套子代理电池、队列、usage、skills、packages、MCP、terminal 全绿） |
| smoke stage 7 sanitized spawn | 未达（随 stage 6 中断） | 留批次收尾门 |
| package:verify | 留批次收尾门（票面允许） | — |

## smoke 阻塞与裁决（master 2026-09-24 03:1x）

两类竞争面，均非 SDK 因素：

1. **usage stage（stage 4）真库活性写入**：幂等 + 零增长断言对真实 `~/.pi/agent/sessions` 连扫两次；批次自身的活跃会话在扫描窗内写库。重载窗口两次失败的硬证据：第二次全套时真库 576→580（两个新 review-spec/review-standards 子代理 transcript + 两个 async run session 文件，由 orchestrator 评审 fanout 创建，落在 stage-4 窗口内）。库静默时独立重扫逐字节幂等（已验证）。master 静默 + 兄弟 worker 收线后，安静窗口两次全套 stage 4 均绿。
2. **electron stage（stage 6）机器负载时序敏感**：负载均值 ~9（三并行 worker + 评审 fanout + WindowServer 37% CPU）下 UI 探针 flake，失败点漂移（empty-state → t105 → t123 → t136，五次五点）；97–400 条子断言/次通过。安静窗口两次尝试仍在不同 UI 探针点失败（t123 @181s、t136 @359s）。t136 两达两败——但渲染层 bundle 与 bump 无关（lock diff 仅 earendil 家族），纯 UI 拖拽为既有环境性 flake，非本票回归。

**master 裁决**：(a) 安静窗口一次性全套（允许 stage 6 重试一次）→ 两次安静尝试仍失败 → 降级 (c)：以分段绿证据 + 两类竞争面文档化为票内验证记录，全套绿 + package:verify 留批次收尾门（根工作区 main 上跑，届时无并行会话），分支照常提交进评审。两次安静尝试（t123、t136）后已按 (c) 收尾。

## 过程记录

- 02:26 开工；02:27–02:28 四文件改动 + npm install（4s，13 包）；02:27–02:28 vitest/typecheck/eslint 绿。
- 02:28:46 拿 smoke 锁 → 全套#1（重载）：stage 4 usage 幂等失败（内容漂移）。
- 02:31:07 释放锁 → 独立复扫验证幂等（库静默时逐字节幂等，0 文件变更）。
- 02:31–02:46 兄弟 worker 持锁（其全套）；02:46:49–02:48:44 全套#2（重载）：stage 4 失败 + 真库 576→580 取证。
- 02:51:24 拿锁；02:52 interop 单段 PASS（双向）；02:57–03:0x electron 单段三跑：t63 guard（我方漏传 PICODE_PI_AGENT_DIR）→ empty-state session_error ×2（时序 flake；直跑 host repro 带与不带 defaults sentinel 均健康：session_created/composer_state/真实回合 REPRO_OK 全通）→ t136 drag。
- 03:1x master 裁决 (a)+(c)；兄弟已收线、master 静默。
- 03:19–03:2x 安静窗口全套#3：stage 1–5 全绿，stage 6 t123 失败（181s，190 断言）。
- 03:2x 安静窗口全套#4：stage 1–5 全绿，stage 6 t136 失败（359s，400 断言）。
- 03:28:41 释放锁；(c) 收尾。

## 收尾

- 分支 `t146-sdk-bump` 提交：package.json / package-lock.json / tests/main/bundled-sdk-versions.test.ts / scripts/package.mjs（四文件，git status 清空）。
- tip sha：`b87d190459c92f051d5c6d9ba830af5ddd28228a`（b87d190，四文件，40+/36−，git status 空）
- 全套 smoke 绿 + package:verify：批次收尾门（根工作区，无并行会话时执行）。
- Linear 由 master 镜像；票面状态翻转归执行会话在 main 上做。
