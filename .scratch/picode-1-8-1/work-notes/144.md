# 144 work notes — forkHost 环境标记剥离（smoke:host 直 fork 路径）

- 开工：2026-09-24 02:27 CST（worktree `wt-144-forkhost-env`，分支 `t144-forkhost-env`，base abfab25）

## 关键步骤

1. 读票 `.scratch/picode-1-8/issues/144-forkhost-env-strip.md` + 对照
   `src/main/spawn-path.ts` `hostForkEnv()`（t142 R2 语义：host fork 是顶层进程永非 child，
   剥 `PI_SUBAGENT_CHILD` / `PI_SUBAGENTS_HERDR_BRIDGE`）与 `scripts/smoke/electron-smoke.mjs`
   的 t142 剥离先例。
2. 改 `scripts/smoke/host-contract-smoke.mjs` `forkHost()`：spawn env 从 `process.env`
   拷贝后 delete 两标记（唯一直 fork seam；全部 12 处调用点走它，含 round K）。
3. 可选项落地：`scripts/smoke/run-all.sh` host-contract 步加
   `env -u PI_SUBAGENT_CHILD -u PI_SUBAGENTS_HERDR_BRIDGE` 前缀（与 electron app smoke 步同型）。
   产品/shared 零改动（diff 仅两个 harness 文件）；`package-lock.json` 的 install 噪音已还原。

## 验证矩阵

| 项 | 命令 | 结果 | 证据 |
|---|---|---|---|
| 受污染 env smoke:host | `PI_SUBAGENT_CHILD=1 PI_SUBAGENTS_HERDR_BRIDGE=1 npm run smoke:host` | PASS exit 0 | /tmp/t144-smoke-tainted.log：rounds A–L 全绿（含 round K stop contract），尾行 `SMOKE PASS host contract smoke complete`；exit 文件 = 0 |
| 干净 env 对照 | `env -u PI_SUBAGENT_CHILD -u PI_SUBAGENTS_HERDR_BRIDGE npm run smoke:host` | PASS exit 0 | /tmp/t144-smoke-clean.log：同全绿 + `SMOKE PASS`；exit = 0 |
| vitest | `env -u PI_SUBAGENT_CHILD -u PI_SUBAGENTS_HERDR_BRIDGE -u PI_SUBAGENTS_PI_CODING_AGENT_PACKAGE_ROOT npm test` | PASS exit 0 | 125 files / 2189 tests 全绿 |
| eslint touched | `npx eslint scripts/smoke/host-contract-smoke.mjs` | PASS exit 0 | 0 errors（run-all.sh 为 shell，eslint 配置本就不覆盖，仅 warning） |
| run-all.sh 语法 | `bash -n scripts/smoke/run-all.sh` | PASS | 无输出 |

注意事项（发现，非本票范围）：
- 本 worker 会话 env 自带 `PI_SUBAGENTS_PI_CODING_AGENT_PACKAGE_ROOT`（runner 继承），
  `tests/main/subagent-runner-root.test.ts` 首例在 base 上同样失败（operator-override-wins
  语义，stash 实证 pre-existing）；剥掉该变量后 2189 全绿。票面验收的 env -u 两变量不含它，
  未动测试（红线：只动 harness）。
- smoke 错峰锁执行中曾被外部 rmdir（02:48 后，疑似他票 cleanup）；已按协议重取锁跑完对照后释放。

## 收尾

- 分支 tip：`bbb38be`（`fix(144): strip stale pi-subagents child markers from the host-contract smoke's forkHost`），git status 干净。
- 未合并（归执行会话 `scripts/merge-ticket.sh 144`）；不 push 不打 tag；未碰 ~/.pi（smoke 只读认证）。
