# 147 — pi-subagents 0.71 探针升位（070→071 更名 + 0.71 增量断言 + 活跑验证）

## 开工

- 开工时间：2026-09-24（worker 会话，分支 `t147-subagents-071`，基座 = main 89528dd（t146 SDK 0.87.1 已合入））
- 环境核对：worktree node_modules `@earendil-works/pi-coding-agent` = **0.87.1**（package.json 实读）；用户级 `~/.pi/agent/npm/node_modules/pi-subagents` = **0.71.0**（package.json 实读，只读核对，零触碰）。
- 0.71.0 源码实证（只读，~/.pi 安装树）：
  - `src/extension/rpc.js:19` SUBAGENT_RPC_METHODS 含 `cost`；`:326` `ping.capabilities.cost = { version: SUBAGENT_COST_REPORT_VERSION }`；`:612-618` cost handler 只读，返回 `collectSubagentCost(...)`。
  - `src/slash/subagent-cost.js:239` 报告形状 `{ version: 1, parent, children, childTotal, total, unresolvedAsyncChildren }`；children 为数组、unresolvedAsyncChildren 为计数——探针只钉 version + 键在场，数值宽松。
  - RPC reply 包装 `rpc.js:675-681`：`{ version, requestId, method, success: true, data: <handler return> }` → 探针 `digRecord(reply.data)` 直读报告。
- 工作树初始状态：`package-lock.json` 带 1 行 npm churn（`ignore` 条目 +`"dev": true`）——与本票无关，提交前 restore。

## 关键步骤

- git mv `scripts/smoke/subagents-070-probe.ts` → `subagents-071-probe.ts`；头注改写（0.71.0 live probe、票 111 起点 + 1.8.1/票 147 升位、三个 0.71 行为注记：dev/unpacked 动态激活 loader 先行【Q6=A 接受现状，探针走 RPC 与直达命令路径不受激活门影响】、打包 worker 默认 fresh 上下文、async-started task/goal 脱敏）。
- 新增断言：ping.capabilities.cost={version:1}；status.json 两级 census（信封键 + steps[] 键，externalProcess presence-tolerant）；一次 cost RPC 调用校验版本化信封（version=1 + parent/children/childTotal/total/unresolvedAsyncChildren 键在场，数值宽松）。
- 内部标识 070→071：tmp 前缀 ×3（cwd/sessions/artifacts）、observer 名 `picode-071-probe-observer`（name + RPC source 两处）、requestId 前缀 `probe071-`、子任务回执标记 `PICODE071OK`（LONG_TASK + steer 文案两处）、正文版本注记 0.70.1→0.71.0（4 处）。
- `package.json`：`smoke:subagents070` → `smoke:subagents071`（outfile `node_modules/.cache/probe071.mjs`）。
- `src/host/subagent-bridge.ts:58`：注释探针路径同步（仅注释）。
- `src/main/index.ts`（票 134 注释）+ `src/shared/subagent-sdk-alignment.ts`（头注）：各补一句——0.71.0 已删 transcript-tools 硬导入（上游 #2377），地板常量维持 0.86.1/0.70.0（守 0.70.x 旧装，语义仍准确）。

## 验证结果

- **活跑探针 PASS exit 0**（`npm run smoke:subagents071` 原样命令，无 env 前缀；用户级 pi-subagents 0.71.0（~/.pi/agent/npm 实读）× 捆绑 SDK 0.87.1（node_modules 实读））：
  - `rpc_capabilities fleetStatus v1 + cost v1 + nonRecoveringSteer + childStatus channel match`（**新增 cost capability 断言过**）
  - `cost_envelope version=1 parent/children/childTotal/total/unresolvedAsyncChildren present children=0 unresolvedAsyncChildren=0`（**新增 cost RPC 版本化信封断言过**——cost 调用在 leg1 前打点，children=0 为真实零态，形状断言不钉数值）
  - 三腿全过：leg1 complete→completed / leg2 RPC stop→stopping→stopped→cancelled / leg3 child-targeted stop→stopping hints 中继、终态 failed 徽标如实
  - `field_census`：0.71.0 信封全键（与调研 census 一致）；`step_field_census`：**新增 steps[] 两级 census 过**（26 键全录）；`external_process_census steps[].externalProcess absent (no external CLI steps ran) — presence-tolerant`（**presence-tolerant 断言过**——scout 子代理非外部 CLI 步骤，缺席如实记录不失败）
  - `PROBE PASS` / `SMOKE_EXIT=0`
- **第一次活跑失败与根因**：`rpc ping: budget of 15000ms elapsed`——worker shell（pi 子代理环境）携带 `PI_SUBAGENT_CHILD=1`，pi-subagents 0.71.0 `extension/index.js:372` 见此标记即整扩展零注册（"an ambient copy … stays inert"）→ RPC 桥不存在。与 t136 留档的「RPC 不应答」同根因，票 142（spawn-path.ts hostForkEnv）已为 host fork 修过同类陈旧标记问题，run-all.sh 两个 stage 也带 env -u。**修复**：探针 setup 段新增 host-environment hygiene 块——启动即 delete `PI_SUBAGENT_CHILD` / `PI_SUBAGENTS_HERDR_BRIDGE` / `PI_SUBAGENT_PARENT_SESSION` / `PI_SUBAGENTS_PI_CODING_AGENT_PACKAGE_ROOT` / `PI_PACKAGE_DIR`（后两者：operator-override-wins 会让陈旧 nvm 全局 pi 抢走 runner 配对，清掉后 ensureSubagentRunnerPackageRoot 才指向捆绑 0.87.1；引票 142 注记）。探针自此从任意 shell 原样可跑。
- vitest：`env -u PI_SUBAGENT_CHILD -u PI_SUBAGENTS_HERDR_BRIDGE -u PI_SUBAGENTS_PI_CODING_AGENT_PACKAGE_ROOT npm test` = 125 文件 / 2193 用例全绿（两次跑均绿——编辑后复跑确认）。
- typecheck（tsconfig.node + tsconfig.web）exit 0；eslint touched 四文件（probe / bridge / index / sdk-alignment）零告警。
- 残留 grep：`git grep -E "subagents070|subagents-070|probe070" -- . ':(exclude).scratch/**'` = 零命中；.scratch 批次记录（票 147 本身/调研/spec/session-prompts/intake/旧批次档案）按「票面不动 + 取证记录」纪律保留历史提及，非活引用。
- smoke 错峰锁：03:40:38 拿到（mkdir 原子成功）→ ps 通道自查空闲 → 活跑 → rmdir 释放；第二次活跑前复查通道仍空闲。
- `package-lock.json` 的 1 行 npm churn（ignore@5.3.2 `"dev": true` 回填）已 restore——票 146 验收记录明示该旗标应处移除态，churn 属本机 npm 重生成翻回，非本票内容。

## 收尾

- 分支提交：`ebf8e6b`（t147-subagents-071，5 文件 +105/−32，rename 相似度 79%），`git status` 干净；不 push、不打 tag、不合并（归执行会话在 main 上驱动）。
- 待执行会话：票 147 状态翻转与 Linear 镜像；探针 env-hygiene 块为票面外的小幅护具修复（票 142 同根因、run-all.sh 同法），已在提交信息与本文档留痕供评审裁量。
- 完成时间：2026-09-24 03:47（本地）。
