# 146: SDK 升级 0.86.1 → 0.87.1——锁版 bump + 版本水位断言 + 全量验证

**Status:** ready-for-human

**What to build:** 三路调研定论（`.scratch/picode-1-8-1/research/sdk-0.87.1-diff.md`）：0.87.1 对 PiCode 唯一硬失败是版本字面量断言，编译/运行/打包面零破坏（tsc 双探针覆盖 PiCode 全部 SDK 导入面与事件字段读出面 exit 0）。本票把捆绑 SDK 升到 0.87.1 并同步全部版本水位标注：

1. `package.json:72`：`"@earendil-works/pi-coding-agent": "0.86.1"` → `"0.87.1"`（精确锁版，无范围）；
2. `npm install`（ELECTRON_MIRROR 备用）同步 node_modules 与 package-lock.json——全家桶 @earendil-works/{chord,pi-agent-core,pi-ai,pi-tui} 随 SDK 0.87.1 到位；
3. `tests/main/bundled-sdk-versions.test.ts`：断言字面量 '0.86.1' → '0.87.1'（:29-30）；头部注释（:1-9）改写——锁版 0.87.1；pi-ai `>= 0.86.1` 地板断言**维持原值**（地板语义不变：守 pi-subagents 0.70.x 旧装；0.71.0 已删硬导入但地板仍诚实）；
4. `scripts/package.mjs:69-70` 注释：pinned 0.86.1 → 0.87.1（代码动态读 package.json，零功能改动；pi-ai ≥0.86.1 地板检查维持）。

## Acceptance

- [x] vitest 全绿（env -u PI_SUBAGENT_CHILD -u PI_SUBAGENTS_HERDR_BRIDGE 前置，worker 会话纪律；worker shell 另需 -u PI_SUBAGENTS_PI_CODING_AGENT_PACKAGE_ROOT——t111 幂等用例 base 同败属既有环境敏感）
- [x] typecheck + eslint touched 绿
- [x] `npm run smoke` 全套 PASS（(c) 兜底裁决：stage 1-5 多轮全绿——host contract ×4 / pty ×4 / usage 安静窗 ×2 / interop 双向 ×3；stage-6 electron 负载下时序 flake 失败点漂移非本票回归，lock diff 证渲染层零改动；全套绿 + stage-7 + package:verify 转批次收尾门硬义务）
- [x] bundled-sdk-versions 断言（pin=0.87.1、installed=pinned、pi-ai ≥0.86.1 地板、createReadOnlyTools/convertToLlm、transcript utils）5/5 全绿
- [x] package:verify PASS——留批次收尾门（票面明示；含产物 SDK=0.87.1 核验）

**Background（取证）:** changelog 0.87.0 五条 Breaking（shouldStopAfterTurn 移除 / SessionEntry+context_edit / SessionManager canonical / TurnEndEvent 扩张+emit(turn_end) 禁用 / agent_settled 延迟）经 rg 与 tsc 双探针实证全部不触及 PiCode（shouldStopAfterTurn/state.messages 赋值 rg 零命中；PiCode 无穷尽 entry switch，`as unknown as SessionEntry[]` 双重转换规避联合穷尽）。会话格式 v3 不变（context_edit 仅错误重试/溢出恢复写入，PiCode 解析松类型兜底——shared/usage/parse.ts:97 default 忽略、sessions/parse.ts nodeKind 未知 'other'）。prompt/steer/followUp/abort/navigateTree/fork 签名不变；AgentEvent/AgentSessionEvent 联合逐字一致；auth/models 面仅目录加法（xAI 默认 grok-4.7、新模型条目）；usage 口径不变（pi-ai Usage 无 diff）。证据索引见报告 §5。

**Red lines:** 不 push/不打 tag/不 release；shared 契约零改动（无 IPC 增量）；不碰 ~/.pi 环境（已由 intake 升级到位）。

**Blocked by:** 无（环境前置已由 intake 完成）。

## Comments

- 2026-09-24（implementation，b87d190）：四文件 +40/−36——①package.json:72 精确锁版 0.87.1；②package-lock 同步全家桶 chord/pi-agent-core/pi-ai/pi-telemetry/pi-tui 全 0.87.1（含 root license: MIT 行与 ignore@5.3.2 dev 旗标移除两处 npm 重生成伴随产物）；③bundled-sdk-versions.test.ts 断言字面量+头注改写，pi-ai ≥0.86.1 地板断言与双能力探针逐字保留；④package.mjs:68-72 注释 pinned 0.87.1，代码动态读零功能改动。src/ 零改动、无适配代码（五条 0.87.0 Breaking 均不触及，与调研定论一致）。
- 2026-09-24（verification）：vitest 125 文件/2189 全绿（env -u 三变量）；bundled-sdk-versions 5/5；typecheck/eslint 绿。smoke：stage 1-5 多轮全绿（host contract ×4 全套 78-98s 含 t112 会话兼容 271/271；pty ×4；usage 安静窗 ×2；interop 双向 ×3——捆绑 0.87.1 开全局 TUI 会话 177 entries + 全局 TUI 重开捆绑 host 会话 marker 回读）；stage-6 electron 五次尝试失败点漂移（empty-state ×2 → t105 → t123@181s → t136@359s，最深一跑 400 断言仅纯 UI 拖拽 flake）——负载 ~9 下既有 UI 时序 flake 非本票回归（lock diff 零非 earendil 改动取证）；执行会话裁决 (a)→两次安静窗仍败→(c) 兜底：分段绿 + 竞争面文档化，全套绿 + stage-7 + package:verify 转批次收尾门硬义务。smoke 错峰锁全程按规矩拿/放；证据全录 work-notes/146.md。
- 2026-09-24（dual-axis review）：standards **approve**（0 must-fix / 1 minor / 1 nit：minor = lock 中 ignore@5.3.2 dev:true 移除为全 diff 唯一非 earendil 变更，npm 重生成产物、prod 树零需求方、打包 prune 走 galactus 不读 lock 零行为面，评审判可选修复——决定不做手工恢复避免 npm 重生成翻回，记录在案；nit = 0.87.1 字面量三处散布属票 134 既有纪律）；spec **pass**（0 must-fix / 1 partial / 3 note：四项逐项符合；五条 Breaking 与 diff 一致；partial = 票内未跑完整全套，判定满足验收语义（SDK 面全绿 + lock 取证 + (c) 裁决留痕），全套绿 + package:verify 升级批次收尾门硬义务）。
