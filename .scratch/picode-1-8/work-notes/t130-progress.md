# t130 fork auto-name progress

本票目标一句话：handleFork fork 落地后即刻自动命名——源有名 → "Fork of <源名>"；源无名 → "Fork of <侧栏标题投影>"（shared/sessions/parse.ts 既有投影）；只写 fork 自己的会话文件（session_info 既有机制），session_renamed + 侧栏索引刷新照旧，用户可再改名。

## Log

- 2026-09-23 阶段=实现落地：Seam-1 投影抽取 + fork 命名实现完成。变更：①src/shared/sessions/parse.ts —— summarizeSession 的 title 逻辑抽取为导出纯函数 sidebarTitleProjection(name, entries)（既有投影单一代码路径，索引扫描器与新命名两用）；新增 forkAutoName(sourceName, entries) = truncateTitle("Fork of " + sidebarTitleProjection(...))，TITLE_MAX_CHARS(80) 口径截断。②src/host/index.ts handleFork —— fork 前取源名 manager.getSessionName() ?? null + rawEntries(manager)；fork 落地后 runtime.session.setSessionName(autoName)（写 fork 自己文件，session_info 既有写入面）；随后 announceCurrentSession(true)（携带 name+tree）+ send session_renamed（渲染层既有索引刷新链路）。源文件零改动。③新增 tests/shared/sessions-fork-name.test.ts：命名投影表驱动（有名/无名→首条用户消息/skill 序言剥离/空源 New Task/超长截断 80/多行折叠）+ 投影与索引扫描器同源断言。证据：typecheck 绿；vitest 119 文件 2028 测试全绿（含 sessions-parse 回归）。
- 2026-09-23 阶段=smoke 段落地：src/main/smoke.ts 新增 ticket-130 段（ticket-129 段后、Quit 前）：seed 两份会话文件（有名腿 = user/assistant 轮 + session_info 显式名；无名腿 = 无 session_info，侧栏标题 = 首条用户消息）→ 真实 host resume_session → fork_session 锚在 settled answer（position 'at'，与 UI Fork 同语义，零模型调用）→ 断言：fork session_created.name = "Fork of …" 两腿；session_renamed 事件（scoped 到 fork 新 id）；fork 自身文件最新 session_info 落盘 = 自动名（ADR-0002 纪律磁盘证据）；侧栏行标题 + 顶栏标题 = 自动名；改名编辑器预填 = 自动名；输入新名 Enter → session_renamed ack + 两侧面显示新名（自动名可覆盖）；源文件字节级零改动。typecheck 绿；vitest 全 2028 绿（曾有一次 2 例偶发失败、后续 5 连跑不复现，与 diff 无交集的时序 flake）。
- 2026-09-23 阶段=跑 electron smoke：ps 自查无其他 electron/PiCode 进程；开始 npm run smoke:electron（全量，含真实模型调用阶段）。
- 2026-09-23 阶段=取证完成：electron smoke 前移位 run 两腿全绿（fork_auto_name_{named,unnamed}_{announce,renamed_event,session_info_written,surfaces,rename_overrides,source_untouched}_ok + done），随后死于 t44 焦点（已知环境事实②，与 diff 零交集；死因是焦点而非内容错位——聚焦恢复胶水生效）。smoke stage 已复原终位（ticket-129 段后）逐字节一致（含 thinking seed + 快照移位两处修订），临时胶水已删，host 的 T130-DEBUG 打印已撤。
- 2026-09-23 阶段=既有崩溃修复（随票窄修）：subagent-bridge 单例捕获 pi.events 在 in-host fork 会话替换后 stale，渲染层 fleet RPC 撞上替换窗口 → SDK stale-ctx 守卫抛错 → uncaughtException → host 退出（smoke 2/4 复现，栈取证：'This extension ctx is stale after session replacement or reload'）。修复 = requestRpc 对 stale bus 降级 null（既有 always-answers 契约）+ 消除悬挂 timer/unsubscribe；vitest 新用例过（stale bus → status available:false / steer ok:false）。修后 electron smoke 两腿全绿即含此修复的验证。
- 2026-09-23 阶段=host-contract 对照：Round K（t101 live stop）环境性失败（pi-subagents stop RPC 超时）——stash 掉 subagent-bridge 改动后同样死在同一断言，与本票 diff 无关，留档不追；Round B（rename/tree nav/fork/fork history）全过 = fork 链路零回归。
- 2026-09-23 阶段=终态：typecheck 绿；vitest 2029 全绿（新增 sessions-fork-name 7 例 + subagent-bridge stale 1 例；首跑曾 2 例偶发 flake 后续 5 连跑不复现）；票 Status → ready-for-human，Comments 记自评双轴 + 前移位取证披露 + 既有崩溃修复披露 + Round K/flake 留档。提交分支 t130-fork-auto-name（sha 见下一条）。
- 2026-09-23 阶段=提交完成：实现提交 76e0e3b（7 files, +437/−31）；翻票提交随后（Status ready-for-human + Comments tip sha 76e0e3b）。progress 文件保持 untracked。
- 2026-09-23 阶段=完成报告截图：第二次前移位取证 run（smoke-6）两腿再次全绿（含 subagent-bridge 修复后稳定），capturePage 于有名腿 surfaces_ok 同帧截图 → /Users/liaokechen/PiCode/.scratch/picode-1-8/work-notes/t130-fork-auto-name-sidebar.png（侧栏行 + 顶栏 = "Fork of PICODE_130_NAMED_SOURCE_NAME"）。smoke.ts 已 git restore 回提交版（终位、无胶水、无 capture，逐字节一致——git status 干净验证）。typecheck 绿。
- 2026-09-23 终态汇总：分支 t130-fork-auto-name（基于 a64cac1；main 已前进至 5120e7c，merge-tree 模拟合并干净无冲突——唯一交集文件 src/main/smoke.ts 自动合并，t117 段在不同位置）。提交：76e0e3b（实现）+ e6db7fb（翻票）。票 Status=ready-for-human。遗留：①t44 焦点环境阻塞（已知事实②，两次前移位取证披露在票 Comments）；②host-contract Round K（t101 live stop）环境性失败，stash 对照证明与本票 diff 无关；③vitest 首跑 2 例偶发 flake 后续不复现。
