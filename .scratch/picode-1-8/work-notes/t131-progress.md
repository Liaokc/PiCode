# t131-progress — History 0 rows 复现定位（fork 会话第一复现场景）

- 票：.scratch/picode-1-8/issues/131-history-zero-rows-repro.md
- 分支：t131-history-zero（基线 main 961f560）
- 本票目标一句话：dev app 插桩复现并定位 Branch History「0 rows」缺陷（第一复现场景 = fork 会话），最小修复，electron smoke 固化 fork 会话 History 行数断言。

## 事件记录

- 2026-09-22 (开工)：阶段=开工。已读票面 + 任务文本 + CONTEXT.md + spec R1（R14 是设置入口票，本票对应 R1）+ ADR 0003/0006。node_modules 就绪（SDK 0.86.1），分支 t131-history-zero @ 961f560，工作区干净。
- 2026-09-22 (静态链路核对)：已读 host/index.ts（sendTree/treePayload/announceCurrentSession/handleFork/request_tree case）、host-supervisor.ts（announce 重绑 + emitScoped）、session-registry.ts（session_tree 按 scope 落账 + applyAnnouncement 树置空）、App.tsx（sendFocused/onHostEvent/TreePanel 开合）、TreePanel.tsx。静态路径四点全通——与票面排除清单一致，缺陷在运行时。
- 2026-09-22 (插桩落地)：临时 `[t131]` 插桩四处——host/index.ts（sendTree try/catch+节点数、request_tree 到达、announceCurrentSession）；host-supervisor.ts（session_command 路由查找、session_created announce 重绑、转发打标）；App.tsx onHostEvent（recv+fold-check、window.__t131Logs）；TreePanel.tsx（render rows）。插桩随修复移除。复现驱动方式：无 GUI 自主操作能力 → 用 Electron remote-debugging-port + CDP executeJavaScript 驱动真实 app（与 smoke.ts 同 DOM 驱动手法），种子会话文件免模型调用。build 绿（out/ 含插桩）。
- 2026-09-22 (复现成功 + 根因定论)：**第一验收项完成**。840 条目链式种子会话（对齐 wrap-up 会话 840 条）稳定复现：History = 0 rows / "This session has no entries yet"。四点插桩证据链：① host `request_tree` 到达、`sendTree ok totalNodes=841 roots=1`（host→main 子进程 IPC 为 JSON，深度容忍，发送成功）；② supervisor 正常转发 `session_tree`（jsonSize=149667 depth=1690），`webContents.send` 不抛异常；③ renderer **从未收到** `session_tree`（recv 日志缺失，后续 request_tree 的响应同样丢失），而同批 `history_loaded`（144KB、depth 5）正常到达；④ registry tree 恒 null → TreePanel 诚实空态，focusedId 全程一致。对照实验（同 841 节点、同 ~145KB）：广度型（210 根、depth 12）→ 到达并渲染 631 行；链式（depth 1690）→ 丢弃。阈值定位：300 条目（wire depth ~610）OK，400 条目（~810）丢失。**根因 = session_tree 载荷的 children 嵌套深度（每条目 ~2 层）超出 Electron main→renderer IPC（V8 structured clone/Mojo）序列化深度限制，消息被静默丢弃**。两个症状同根：长会话 History 恒 0（发送/终止只是查看时机）；深会话的 fork 文件同为深链 → fork 会话同样丢并（fork 的会话 history 没信息）。修复选型（最小落点）：wire 载荷改扁平（nodes 按文件序 + parentId 链，深度 O(1)）——host 扁平化发送、renderer 唯一 wire 消费点（session-registry fold）重建嵌套 canonical 树；下游（registry 状态/TreePanel/tree-view/视觉 harness）零改动；supervisor 纯转发零改动；host-contract smoke round L 更新为扁平 wire。无并行在飞工单消费 session_tree（已核实 issues 目录）。证据在 /tmp/t131-app.log + driver 脚本 /tmp/t131-driver*.mjs。
- 2026-09-22 (修复落地)：wire 扁平化修复落地——shared/sessions/types.ts 增 SessionTreeWireNode/SessionTreeWirePayload（parentId 链、深度 O(1)，带约束注释）；新纯模块 shared/sessions/tree-wire.ts（sessionTreeToWire/sessionTreeFromWire，迭代重建不递归）；contract.ts session_tree 事件改 wire 载荷；host treePayload 扁平化发送；session-registry fold（wrapped+legacy 两路）重建嵌套 canonical 树；下游（TreePanel/tree-view/视觉 harness）零改动。全部 [t131] 插桩已移除。测试：新 tests/shared/sessions-tree-wire.test.ts（往返/深度常数/600 深链重建/乱序降级/wire 形状）；session-registry.test 增扁平 wire fold 用例。host-contract smoke round L 改扁平断言（children 禁现 + parentId 校验）。vitest 全绿（2061）+ typecheck 绿。真实 app 验证：840 链 → 842 行（文件含早前 driver 追加的 2 条）；430 链 → 430 行；fork@e200 → 201 行（200 链 + 1 session_info 自动命名行）+ 唯一 current tag——与 smoke 期望一致。新增 electron smoke 段 history_deep（430 链 + fork@e200 断言 201 行）已落 smoke.ts（ticket-43 段之后），driver 干跑已验证期望值。
- 2026-09-22 (smoke:host 两跑)：第一跑死在 round A 'A agent_end 1'（abort 与模型收尾相撞：日志显示 count-to-twenty 轮在 rename+abort 前已完成、agent_end 早于 waiter——与 diff 零交集的时序 flake）；第二跑 round A 绿、死在 round K（pi-subagents stop RPC 超时）——ps 证实 wt-119 的 electron smoke 正在同时跑（3 个 pi-subagents runner 进程）——已知跨分支资源争用。等待对方 smoke 结束后重跑 smoke:host 与 smoke:electron。
- 2026-09-22 (smoke:host 四跑 + stash A/B)：第三跑再死 round A；stash 掉全部本票改动跑基线＝死 round K（pi-subagents stop RPC 超时，与票 130 Comments 先例同现象）——Round A abort 竞态与 Round K 环境超时均与 diff 零交集且基线复现，留档不追；跑至 K 的趟次 Round B（含 fork 链路）全过。
- 2026-09-22 (electron smoke 取证绿)：history_deep 段按 t129/t130 手法临时前移至 t44 之前取证：`history_deep_{source_rows_ok 430, fork_announced, fork_rows_ok 201, fork_rows_ok 201, done}` 全绿；随后死于 t44 焦点（已知环境阻塞）。复原终位逐字节一致（git diff 单一插入块验证）。注：前移位时 fork 会话会接住 empty_state 段的 pending prompt（App 既有语义，终位无此交互）。
- 2026-09-22 (提交 + 翻票)：提交 c57e878（实现：tree-wire 模块+契约 wire 载荷+host 扁平发送+registry 重建+smoke 两处+测试三处）与 2a2b9d7（票 Status→ready-for-human + branch tip Comments）。vitest 2062 全绿 / typecheck 绿 / eslint 干净。截图留证：/Users/liaokechen/PiCode/.scratch/picode-1-8/work-notes/t131-history-deep-session-842rows.png（840 链会话 History 842 rows，修复后真实 app）。终态：票 ready-for-human，分支 t131-history-zero tip = 2a2b9d7（实现提交 c57e878），待主 Agent 跑 scripts/merge-ticket.sh 131。遗留：①smoke:host Round A abort 竞态与 Round K pi-subagents 超时为环境/既有 flake（基线复现，已留档）；②本环境 electron smoke 无法越过 t44（焦点 steal），history_deep 终位段以等价前移位取证；③极深会话（>数千条）的 JS 递归上限属 buildSessionTree 既有约束，非本票范围。
