# t120-rail-anchor progress

- 本票目标（一句话）：navigator-rail 纯模型锚定决策表扩展——吸底（isAtBottom 口径）锚定最新回合（含 live），非吸底维持探针规则（0.35 不动）；CONTEXT.md 导航轨词条修订随票。
- 阶段 = 开工

## 事件

- 2026-09-22 开工：已读任务文本 + context.md + 票面 120（Status=ready-for-agent）。证据：`.scratch/picode-1-8/issues/120-navigator-live-anchor.md`、`context.md`。
- 2026-09-22 取证结论：根因与票面一致——`src/shared/navigator-rail.ts:131` `anchoredTurnId` 探针规则（top ≤ scrollTop+0.35H 的最末条）在吸底时够不着刚起步的 live 回合用户气泡（Working 容器 + composer 占底部 ~25%），锚定留在上一轮。证据：`src/shared/navigator-rail.ts`（anchoredTurnId）、`src/renderer/src/components/ChatView.tsx:117-192`（吸底/arrival pin 机制）。
- 2026-09-22 方案定稿：① `shared/navigator-rail.ts` 新增纯决策 `railAnchoredTurnId(anchors, geometry, probeY, viewport: ScrollSnapshot)`——`isAtBottom(viewport)`（复用 scroll-stay 判据，distance<1，严于 160px 带）→ 最后一个 anchor（live 含内）；否则现行探针规则 `anchoredTurnId` 原样（0.35 不动）。② `NavigatorRail.tsx` measureAnchored 改为只消费新决策（测量 geometry + snapshot 传入）。③ 表驱动 vitest 进 `tests/shared/navigator-rail.test.ts`（吸底/非吸底 × live/落定 × 单/多回合 + 带内非到底边界 + 空转录；探针回归用例全保留）。④ smoke：ticket-94 先例（emitContractEvent 合成 session_created(resumed)+history_loaded+live 事件，零模型调用），四格矩阵 e2e（live×吸底 / live×上翻 / settled×上翻 / settled×吸底）。⑤ visual harness 新建 `visual-rail-anchor.ts`（ra1-at-bottom-live / ra2-scrolled-up 两帧，pi18-* 参照帧未复制——已查 .scratch/compare 无 pi18*）。⑥ CONTEXT.md 导航轨词条补锚定规则。渲染词汇（scaleX/opacity）零改动。
- 2026-09-22 状态翻转：票 120 Status → claimed（本 worktree 副本）。
- 2026-09-22 实现落地：① `src/shared/navigator-rail.ts` 新增 `railAnchoredTurnId(anchors, geometry, probeY, viewport)`（isAtBottom→最后一个 anchor；否则 `anchoredTurnId` 探针规则不动），`anchoredTurnId` 原样保留；② `NavigatorRail.tsx` measureAnchored 改为消费新决策（deps 增 anchors）；③ `tests/shared/navigator-rail.test.ts` 新增决策表 describe（吸底/非吸底 × live/落定 × 单/多回合 + 160px 带内非到底边界 + distance=1 边界 + 空转录 + 探针规则回归对照）——rail+scroll 两文件 39 测试全绿；全仓 vitest 1977/1978 绿（唯一失败 `tests/main/subagent-runner-root.test.ts` 为环境性预存：root main worktree 同样失败，与本票无关）；typecheck 绿；lint 新增代码零告警（预存 11 错均为未触碰文件）。④ `src/main/smoke.ts` 新增 ticket-120 阶段（ticket-94 先例：emitContractEvent 合成 session_created(resumed)+history_loaded 三高回合 + live 注入，零模型调用；四格矩阵：live×吸底 / live×上翻顶 / settled×上翻中位（3/4 回合间探针线确定性放置）/ settled×吸底）。⑤ `src/main/visual-rail-anchor.ts` 新 harness（ra1-at-bottom-live / ra2-scrolled-up 两帧 + rail-anchor.json），接线 visual.ts stand-down + index.ts（isolate+start）+ package.json `visual:rail-anchor`。⑥ CONTEXT.md 导航轨词条已补锚定规则（吸底分支 + Seam-1 清单句更新）。
- 2026-09-22 阻塞解除：wt-121 的 electron smoke 约 15 分钟后结束，通道清空（ps 复查 0 进程）。
- 2026-09-22 visual harness 结果：`npm run visual:rail-anchor` 全部断言过（exit 0）：ra1-at-bottom-live（ticks=4、focused=m6 最新 live 回合、distFromBottom=-0.5 吸底、labels=[Worked×3,Working]、jumpVisible=false）；ra2-scrolled-up（focused=ra120-u1 首回合、scrollTop=0、jumpVisible=true）。帧：`.scratch/visual/ra1-at-bottom-live.png`、`.scratch/visual/ra2-scrolled-up.png`，转储 `rail-anchor.json`。修正 harness 日志键名小瑕疵（logState ra1/ra2 → liveBottom/scrolledUp）。pi18-* 参照帧不存在（.scratch/compare 无），两帧独立成立。
- 2026-09-22 待办：electron smoke（ps 已自查 0 进程，开跑）、自评审双轴、提交、翻 ready-for-human。
- 2026-09-22 smoke 迭代 1：run1 在 ticket-105（⌘J 终端聚焦，本批票 132/R6 预存回归）fail-fast；run3 证实 ticket-105 为环境性偶发（第二次全过）但 ticket-46 阶段 ③「点击首 tick 后焦点跟随」失败——诊断：新规则下短转录（不溢出）恒为吸底→锚定最新回合，点击跳转无法离底；修复 = ticket-46 阶段首条 prompt 加确定性空白填充。首修复（尾部填充）无效——Composer 发送路径 `.trim()`（Composer.tsx:587）剥掉尾部空行；改为中段填充后通过。
- 2026-09-22 smoke 迭代 2（run5）：ticket-46 全过（nav_rail_click_jump_ok / narrow_hidden_ok）；**ticket-120 阶段四格全绿**：nav_live_anchor_scene_ok / live_bottom_ok / live_scrolled_ok / settled_scrolled_ok / settled_bottom_ok / done。后续 ticket-90（子代理目录，与本票零代码交集）在「live run never flipped to a Running badge」失败——run6 重跑判定偶发/预存中。诊断 harness（visual-rail-diag）已删；诊断期间被基础 visual flow 重拍的 3 张跟踪帧已 git checkout 还原。
- 2026-09-22 诊断实证（临时 harness，已删）：两回合回放（首回合 40 空行）点击首 tick 后 scrollTop 781→2、dist 779、焦点正确落到首 tick——模型/组件机制完全正常，失败全在 smoke 场景假设。
- 2026-09-22 smoke 终态：run6 我的阶段再次全绿（四格+done，两轮连续）；ticket-90 本轮通过（run5 为偶发）；新失败在 ticket-91（图片预览，零码交集、非确定性——与 run5/run6 分别挂不同后续阶段互证为环境性/预存类，均在本票阶段之后）。全量 vitest 1977/1978（唯一失败 tests/main/subagent-runner-root 为环境性预存，root main 同样失败）；typecheck 绿。
- 2026-09-22 终态：实现提交 `7d233fe`（含 13 文件：模型/组件/测试/smoke 阶段+票 46 涟漪修复/visual harness+接线/CONTEXT.md 词条/票面勾选与 Comments/两帧+json）；簿记提交 `5e43a3e`（Comments 记实现 sha）；分支 tip = `5e43a3e`；工作区干净。票 120 Status = ready-for-human。已向主 Agent 报告完成。