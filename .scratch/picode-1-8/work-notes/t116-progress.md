# t116 progress — 展开态输入高度稳定（typing-commit 按 expandState 分流）

本票目标一句话：composer typing-commit 高度路径按 expandState 分流——展开态输入/删除不缩高（重投影 composerExpandHeight），收起态 auto-grow 74–160 不回归；缩矮唯一触发 = toggle/Esc/⌘E/sent。

## 事件记录

- 2026-09-22 01:50:52 +0800 — 阶段=开工。已读票面 116、CONTEXT.md 术语（输入展开）、spec R11。分支 t116-expand-height，票 Status=ready-for-agent → 即将翻 claimed。证据：.scratch/picode-1-8/issues/116-expand-height-stable.md

- 2026-09-22 01:58 +0800 — 阶段=方案定稿。根因确认：src/renderer/src/components/Composer.tsx 高度 layout effect（约 L275-300）typing-commit 路径无条件跑 `composerAutoGrowHeight(el.scrollHeight)`（钳 74–160），不检查 expanded → 展开态每键被压回 ≤160 而 expandState 仍 'expanded'（票面图11吻合）。修法：① expand.ts 增表驱动纯函数 composerTypingHeight(expandState, {contentPx, mainAreaPx})——expanded 行 = composerExpandHeight(mainAreaPx)（不读 content），collapsed 行 = composerAutoGrowHeight(contentPx)（不读 mainArea）；状态机 EXPAND_TRANSITIONS/reduceComposerExpand 零改动。② Composer.tsx typing-commit 分流：expanded 分支单次写重投影（不走 auto 往返——auto 穿透会杀掉 armed glide 动画、且无需测 content），collapsed 分支保留既有 auto-reset 仪式，两分支都经 composerTypingHeight 定高。③ tests/shared/composer-expand.test.ts 增决策表。④ src/main/smoke.ts 在 ticket-49 stage 后增 ticket-116 stage：展开态输入/删除（含清空）高度稳定、窗口 resize 重投影、发送收回回归（一次真实模型往返）。证据：Composer.tsx L275-300、expand.ts、smoke.ts L6022-6270。

- 2026-09-22 02:00 +0800 — 修复落地（renderer + seam）。expand.ts 增 composerTypingHeight（表驱动，状态机零改动）；Composer.tsx layout effect typing-commit 按 expanded 分流（expanded 单写重投影、无 auto 往返；collapsed 走既有仪式，均经 seam 定高）。tests/shared/composer-expand.test.ts 增 4 组决策表用例（44 tests 全绿）；typecheck 全绿。证据：expand.ts composerTypingHeight、Composer.tsx L300-340、vitest 输出。

- 2026-09-22 02:10 +0800 — smoke 第 1 次（ticket-116 stage 首跑）：前序全过（含 ticket-49 composer_expand 全组 ok）；本 stage 「long draft」probe 失败但 diag 显示 h=424=projected（高度未缩）——根因是 probe 前提错：12 行草稿（~284px）撑不满 424px 展开盒，scrollHeight>clientHeight 不成立。修法：草稿加到 40 行保证任何行高下都溢出（内部滚动断言才有效），失败信息改为如实描述。单行输入稳定 probe 已通过（operator 逐字复现场景）。证据：smoke 输出 `composer_typing_116_collapsed_band_ok` + FAIL long draft DOM {"h":424,"projected":424}。
- 2026-09-22 02:22 +0800 — smoke 第 2 次：更早的既有 stage slash_gate（ticket 38，本票零改动）flaky 失败（composer busy、value 空、toast 未出，事件流显示 8f365b 思考流未落 agent_end——模型回合竞态；wt-121 同期在跑 smoke 抢同一模型 API）。第 1 次运行该 stage 通过 → 判定环境性 flake，非本票回归。重跑验证中。

- 2026-09-22 02:35 +0800 — smoke 第 3 次关键发现：本票 stage 六项全绿（collapsed_band / input_stable / delete_stable / resize_reproject / send_collapse / done），ticket-49/81 回归全绿；但后续既有 ticket-91 stage 起点断言 74px 失败。根因（非本票产品代码问题，是 smoke 既有 stage 的隐性清理缺陷被本修复暴露）：ticket-81 stage 的 reduced-motion leg 最后把 composer 展开后从不清回，旧代码下其收尾 composerClearJs（value 变更）会经旧 bug 把高度压回 74——ticket-91 的起点断言一直踩着这个 bug 通过。修法（smoke 侧最小修）：ticket-81 收尾在清空草稿/附件后补一次 Escape（expand 状态机自己的收起路径）+ 显式探针 74/aria-expanded=false，兑现其注释承诺的「Leave the composer clean for the later stages」。已审计全部后续 stage：无第二处同类隐性依赖（clientHeight===74 断言仅 49/116/81/91 四处；expand 交互止于 81）。证据：smoke 第 3 次输出、smoke.ts L6684（reduced-motion 展开）与 81 收尾、L6732（91 起点断言）。

- 2026-09-22 02:40 +0800 — smoke 第 4 次：slash_gate 过；既有 ticket-88 stage（HTML 预览沙箱，与 composer 无关）flaky 失败 img=false。根因（机制明确）：沙箱 iframe 内联脚本在解析期同步执行并读 img.naturalWidth，相对路径 PNG 异步加载——高负载下脚本先跑（css 恒过是因为样式表阻塞脚本）。run1/3 过、run4 败，纯负载竞态。修法（smoke 侧最小加固，非产品代码）：fixture 脚本在图片 load/error 后补发第二份 probe，父侧改为等待「img=true 的已落定 probe」而非首条快照；css/sandbox 检查仍取首条（确定性）。证据：smoke.ts ticket-88 fixture script 与 probeArrived 段、第 4 次输出 css=rgb(255,240,224) img=false。

- 2026-09-22 03:05 +0800 — smoke 第 5/7 次关键结论：本票 stage（composer_typing_116 六项）+ composer 全家回归（49/81/91）在 run3/5/7 三次全绿；run6 的 81-band 一次性 flake（attach 0 + 莫名回合，run7 未复现）。ticket-90 stage「live run never flipped to Running badge」在本分支 run5/7 两次失败——但取证证明为**跨分支预存故障**：wt-120/smoke5、wt-121/run2、wt-121/base-smoke3（均无 composer 改动）同一晚同一断言失败，事件尾签名一致（mcp_auth 流 + subagent_status）；wt-123 同晚另败于 75/88（88 正是本票已加固的图片竞态，跨分支证实）；wt-120 另败于 105/91/46。结论：当前环境（3 个并行 worktree 抢同一模型 API）下全量 smoke 在任何分支都无法全绿；ticket-90 非本票 diff 所致（本票 diff 仅 composer 高度 + smoke stage，触不到 subagents 面板/host 事件管线）。证据：/tmp/t120-smoke5.log、/tmp/t121-smoke-run2.log、/tmp/t121-base-smoke3.log、/tmp/t123-electron-smoke*.log、/tmp/t116-smoke-run7.log。
- 2026-09-22 03:05 +0800 — 验收态盘点：Seam-1 决策表（expand.ts composerTypingHeight + 4 组 vitest 表）✓；electron smoke 展开态输入/删除稳定（input_stable/delete_stable/空草稿保持 ~424px）✓；收起态 auto-grow 回归（collapsed_band_ok + ticket-49 stage 全组）✓；发送收回（send_collapse_ok）✓；resize 重投影（resize_reproject_ok，真实 setBounds 双向）✓；vitest 1973/1974（唯一失败为预存环境依赖项 subagent-runner-root，干净分支同样失败）✓；typecheck ✓。遗留：全量 smoke 绿被预存跨分支 ticket-90 故障 + 当晚环境阻塞（见上条）。
- 2026-09-22 03:14 +0800 — 收尾（主 Agent 裁决：按完成口径，不追 ticket-90）：实现提交 `1648d87`（expand.ts Seam-1 决策表 + Composer typing-commit 分流 + vitest 表 + smoke stage + 81/88 两处 smoke 层加固）；票面翻转提交 `d2c1244`（Status→ready-for-human，Comments 记 tip sha、两处加固报备、ticket-90 跨分支预存证据指针）。验证链终态：typecheck 绿；vitest 1973/1974（唯一失败预存）；本票 smoke stage 六项 + 49/81/91 三次运行全绿；ticket-90 为跨分支预存故障（wt-120/121 同晚同断言失败），已记录在案不追。visual:expand harness 全部探针过（e1a/e1/e2/e2b 四帧捕获），e2-expand-open.png 为本票 UI 证据：展开态 + 14 行长草稿保持 ~半区高度（~424px），transcript 下推。工单完成。
- 2026-09-22 03:20 +0800 — 补录主 Agent 通道信息：① ticket-90 预存失败定案（票 121 在裸 base 50ba1b4 A/B 复现；根因假设 = fleet RPC 失败 → available:false → foldSubagentStatus 丢快照，subagent-bridge.ts:148 + session-registry.ts:241；并发 subagent 负载相关）已补入票面 Comments 并提交（`21c88a3`）；不追不改。② 主 Agent 即将占用 electron 通道 15–20 分钟（票 121 parity 实证）——本票已收尾无计划内 smoke；若后续任何会话需跑，先 ps 自查等窗口结束。工单状态不变：ready-for-human，分支 tip 见上。
