# t132 Progress — ⌘J terminal focus regression (ticket 132 / spec R6)

**Ticket goal (one sentence):** Reproduce & locate the ⌘J terminal-focus regression on the new-session path (recurrence of the ticket-105 bug), fix it minimally, and build a four-entry ⌘J focus electron smoke (in-session / new-session / boot-empty / bridge-switch-back) as the regression firewall.

**Phase: 开工 (started).**

- [2026-09-22 开工] Read ticket 132 (authoritative), CONTEXT.md terms (终端停靠/焦点纪律), spec R6 (task text said "R21" but ticket 132 maps to R6 — ticket face is authoritative). Branch: t132-terminal-focus, based on main 93a1646.
- [2026-09-22 插桩准备] ps 自查：wt-131 dev app / wt-119 smoke 运行中 → 等待通道；期间完成静态根因假设（focusSeq bump 在无 workspace（cwd=null）或 bump 后 workspace 重挂载时被消费/丢失）+ 临时插桩（TerminalDock T132 logs：seq-diff/double-rAF/refNull/workspace mount/unmount）+ 临时 repro harness（src/main/t132-repro.ts，visual-harness 同型：隔离 userData+store、种子回拨会话、S1 boot空态+create在途⌘J / S2 聚焦会话+create B在途⌘J / S3 对照腿）+ index.ts 临时接线。npm run build 绿。通道已清（ps 复查零 electron 进程）。
- [2026-09-22 复现成功 = 第一验收项达成] t132 repro harness（隔离 userData+store、种子回拨会话、真实 create 流、合成 ⌘J——ticket-105 smoke 先例）证据链（/tmp/t132-run1.log）：
  - S1（boot 空态 + create 在途 ⌘J）= **BUG 复现**：`seq-diff 0->1 tabOpen=true cwd=null refNull=true` → `double-rAF fire refNull=true`（焦点请求打进 null ref，静默丢弃）→ `workspace mount cwd=…t132-demo`（session_created 后 shell 才挂载，无新 bump）→ verdict `{"inShell":false,"tag":"BODY","dock":"terminal"}`——dock 开着终端、焦点在 BODY。
  - S2（聚焦会话 + 同 cwd create 在途 ⌘J）：无重挂载（workspace key 同 cwd）→ 焦点保持在已挂 shell，verdict inShell=true（同 cwd 时现状不翻车）。
  - S3 对照（聚焦会话 + 普通 ⌘J，ticket-105 形）：inShell=true 机制在位。
  - 根因定论：**focusSeq bump 在「无 workspace（cwd=null）」时被消费成 no-op，shell 后到无补偿**——票 105 修复只覆盖「bump 时 shell 已挂载」的路径；新会话/boot 空态入口（bump 早于 shell 挂载）是覆盖缺口。S2 异 cwd 变体（bump 打进旧 shell、随后重挂载丢焦）同根因族，修复需一并覆盖（pending 直至兑现）。
- [2026-09-22 复现补强（/tmp/t132-run2.log）] S2b（聚焦会话 + **异 cwd** create 在途 ⌘J）= 第二失败形状确认：`seq-diff 2->3 refNull=false` → serve 打进旧 shell（active=xterm-helper-textarea）→ `workspace unmount`（session_created 落地、旧 shell 持焦卸载）→ `workspace mount`（新 cwd、无 bump）→ verdict `{"inShell":false,"tag":"BODY"}`。同 cwd（S2）不重挂载、现状不翻车。focusin 监听时间线为空系窗口后台（document not focused 时 focus 事件不派发——verdict 轮询 activeElement 不受影响，证据以内部 seq-diff/serve/mount 日志为准）。
- [2026-09-22 根因定论 + 方案选型] 根因族：**⌘J 焦点请求绑定「双 rAF 时刻恰好挂载的 workspace 实例」**——(a) 请求先于 shell 挂载（boot 空态/create 在途、tabOpen false）→ 打进 null ref 被静默消费；(b) 请求打进旧 shell、随后 session_created 重挂载换掉接收者 → 焦点随卸载落 BODY。修复 = TerminalDock 焦点请求状态机（Seam-1 纯决策 `terminalFocusServeDecision` + 渲染层双 rAF 胶水）：bump→armed；armed 直到有 receiver 才 serve（hold 跨挂载存活）；卸载时上报「持焦」→ 替换 workspace 恢复焦点（除非活 caret 已接管）；dock/panel 不可见即取消（保票 105「桥接可见不抢焦」与「任务切换重挂载不聚焦」——任务切换前必有 click→票 98 归还 composer，旧 shell 不持焦，恢复不触发）。
- [2026-09-22 修复落地 + 验证] 实现（Seam-1 `terminalFocusServeDecision` 9 例表驱动 + TerminalDock 状态机 armed/heldByReplaced + BottomDock `visible` 透传 + TerminalWorkspace 注册回调）：
  - 迭代中发现并修掉两个实现坑：①held 检测不能用 rootRef（React 在 mutation 期已摘 ref）；②**held 检测不能在 passive cleanup 里读 activeElement——passive cleanup 在 DOM 移除之后跑，焦点已落 BODY**；正确落点 = useLayoutEffect cleanup（DOM 仍挂着，`kit.userTerm.textarea === document.activeElement` 为真值）。
  - 修复后 harness 复验（/tmp/t132-run7.log）：S1 boot空态+create在途 ⌘J → `hold`→注册→`serve` inShell=true ✓；S2 同cwd ✓；S2b 异cwd create+⌘J → serve 旧 shell → `register focus=null held=true`（layout cleanup 如实捕获）→ 新 workspace 注册 → restore serve inShell=true ✓；S3 对照 ✓。四形状全绿。
- [2026-09-22 smoke 参数化落地] ①empty_state 阶段插 boot 空态腿（⑤ send 前按 ⌘J——断言 dock 开+「No workspace yet」无 shell；session_created 后断言首个 shell 挂载即持焦，随后还原关闭）；②terminal_focus_105 后新增 `terminal_focus_132` 阶段：参数化 `ensureShellFocused(leg)`（dock=terminal + activeElement=xterm textarea 单一真值），三腿 = in_session（105 leg1 形复跑）/ new_session（tag 旧 xterm → supervisor.createSession 新 tmpdir cwd → 立即 ⌘J → 等 untagged xterm 重挂载 → 断言焦点进新 shell）/ bridge_swap_back（⌥⌘J 桥接显示时隐藏终端不持焦 + ⌘J 切回聚焦）。四入口 = boot 空态 + 这三腿。typecheck/eslint/vitest 2083 全绿。
- [2026-09-22 smoke 取证 + 复原] 全套 smoke 两轮均翻在 t44 user_copy 段（「window never took focus for the real-clipboard click」/「user message blocks never rendered」——已知环境类，与本票 diff 零交集）。按 t129/t117/t125/t130 手法：t132 阶段临时前移至 t44 前（empty_state 后）重跑——**四腿全绿**（boot_empty_open/boot_empty_focus/in_session/new_session/bridge_swap_back 六标记）；随后复原终位（105 阶段后），stage 块逐字节一致验证 True；typecheck/eslint 复验清。
- [2026-09-22 票 Comments 落盘] 复现证据链/根因/修复/smoke 四腿/t44 披露/票 105 覆盖缺口自检复盘/双轴 self-review 全部写入票 Comments（保留原 intake 条目）。
- [2026-09-22 终态] 提交：`6ad6a11` fix(132)（6 文件：dock-model 决策表+9 测试 / TerminalDock 状态机 / BottomDock visible / smoke 四腿 / 票 Comments+Acceptance）+ `43aade9` chore(132) 翻 ready-for-human（实现 tip 6ad6a11 记入 Comments）。分支 t132-terminal-focus 干净。t44 环境堵点四轮如实披露（含票 105 阶段未能重跑的替代证据说明）。vitest 2083/2083、typecheck 双 tsconfig 清、eslint 触碰文件清。插桩与临时 harness 全部移除（src 零 T132DBG 残留）。
