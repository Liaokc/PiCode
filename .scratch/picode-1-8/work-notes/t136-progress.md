# t136 progress — subagent dedicated sidebar

- 2026-09-22 11:59 claimed ticket. Branch t136-subagent-sidebar @ 331922e (= main tip). Read ticket 136 (root .scratch, untracked there), CONTEXT.md, ADR list. Worktree .scratch only has issues up to 134 — will copy 136 ticket into worktree .scratch for branch commit (纪律: 票文件随分支提交).
- Plan: read ADRs 0001–0006 (skim relevant), then code archaeology: App.tsx top-bar entry cluster, SidePanel.tsx (tabs incl. SubagentsTab/SubagentChatTab), subagent-chat-store, onStop/onOpenChat bridge, smoke subagent_dir/subagent_chat legs, visual harness.
- 2026-09-22 12:10 Code archaeology done. Key facts: SidePanel.tsx:189 tabBody hosts SubagentsTab (subagentsDirectory prop) + SubagentChatTab (SubagentChatBridge from SidePanel.tsx); ticket-101 badge = count badge on side-panel toggle (TitleBar.tsx, `.tb-btn-badge`, only while collapsed); smoke stages subagent_dir (smoke.ts:12844) / subagent_chat (13118) / subagent_stop (13398) open the tab via ⌥⌘B + picker card `.panel-tab-card[aria-label="Open Subagents tab"]`; visual-subagents.ts + visual-subagents-chat.ts use the same opening + s101-3 badge frame; PANEL_EMPTY_TABS=['review','subagents'] in layout-model.ts; panel width persists via preferences.panelWidth; shell flags in layout-model ShellUiState.
- Design decided: (a) ShellUiState gains subagentPanelOpen + open/close/toggle-subagent-panel actions; the OPENING legs fold the other right pane (reducer, table-tested). (b) New shared/subagent-panel-model.ts: subagentPanelReducer = guard (fixed 'subagents' tab never closes; foreign kinds ignored) delegating to panelReducer over PanelState — identical tab semantics. (c) Width inheritance: pure planRightPaneWidthSwap(prev,next) in layout-model + ONE useLayoutEffect in App applying it before paint (handles ALL dispatch sites: ⌥⌘B, deep links, entry click, badge click — no per-site wiring, no flicker). (d) New SubagentPanel.tsx mirroring SidePanel geometry (own CSS vars --subagent-panel-w/-content-w; reuses .panel-* classes). (e) TitleBar: subagent entry right of side-panel toggle (old settings slot), BotIcon glyph, tooltip "Subagents", badge (count, only while subagent panel collapsed), data-testid subagent-panel-toggle / subagent-badge. (f) SidePanel loses SubagentsTab/SubagentChatTab/bridge props; PANEL_EMPTY_TABS → ['review']. (g) preferences.subagentPanelWidth persisted like panelWidth. Smoke: redirect 90/99/101 stages to the new sidebar + new t136 mutex stage; visual harnesses redirected; new visual frame = redirected s9-1 (directory in new sidebar).
- 2026-09-22 12:36 Implementation core done, vitest 2175/2175 + typecheck green. Changed: layout-model.ts (subagentPanelOpen flag + open/close/toggle actions with mutex fold + planRightPaneWidthSwap), new subagent-panel-model.ts (guard reducer over PanelState: fixed tab never closes, foreign kinds dropped), preferences.ts (+subagentPanelWidth), icons.tsx (BotIcon), TitleBar.tsx (subagent entry right of side-panel toggle, badge only while collapsed, data-testid subagent-panel-toggle/subagent-badge; side-panel button back to plain toggle), SidePanel.tsx (subagent cases/props removed; picker = Review only), new SubagentPanel.tsx (mirror geometry, hosts SubagentsTab + SubagentChatTab, SubagentChatBridge moved here), App.tsx (state wiring, boot seed, subagentsTabActive retarget gated on panel open, swap useLayoutEffect, dispatchSubagentPanelPersisting, bridge onOpenChat → subagent panel + open-subagent-panel, handleToggleSubagents activates fixed tab), app.css (.subagent-panel block + shared selector extensions + sidebar max-width yields to both right panes), main/index.ts fakePreferences, tests (layout-model mutex+planner tables, subagent-panel-model, preferences, settings-service).
- Next: smoke.ts ticket-90/99/101 stage redirects + new t136 mutex stage; visual-subagents.ts / visual-subagents-chat.ts redirects; then electron smoke + visual frames.
- 2026-09-22 12:50 smoke.ts redirects done: 90/99/101 stages open via entry click (normalize: collapse subagent panel if open → entry click → assert `.subagent-panel:not([data-closed])` + fixed tab `[data-testid="subagents-tab"]`); tab labels/close/reactivation scoped `.subagent-panel`; 101 normalization collapses BOTH panes, badge probes → `[data-testid="subagent-badge"]`, badge-click opens onto directory tab; NEW stage `subagent_mutex_136` after subagent_stop (dragPane synthetic PointerEvent helper; ①entry→fixed tab ②manual collapse no cross-open ③⌥⌘B + drag side 560 ④swap: entry folds side, subagent panel = 560 ⑤collapse ⑥reopen 560 ⑦drag subagent 640 → ⌥⌘B symmetric swap side = 640 ⑧collapse ⑨reopen 640; cleanup collapses both). visual-subagents.ts / visual-subagents-chat.ts openings → entry click; multi-tab/close selectors scoped `.subagent-panel`. npm run build green (9.1s).
- 2026-09-22 13:05 Electron smoke attempts (ps self-check clean each time):
  - run1/2: full suite died at multi_session (ticket-28, documented flaky family): the model's count-to-150 turn completes in ~5s (agent_end before the decoupled/refocus probes). Session file proves the full 1..150 was emitted — model-speed race, zero diff intersection. (Also: electron binary had to download first via npx.)
  - run3 (count prompts temp-lengthened 150→3000): multi_session ALL GREEN, died at ticket-105 trusted-keystroke echo (t44 environmental focus class; stage already re-steals focus; renderer reported focused, keystroke never echoed).
  - CONCLUSION: cannot reach the subagent stages through the full suite in this environment without temp patches.
- 2026-09-22 13:10 Built TEMP verification harness (all REVERTED before commit): T136_SUBAGENTS_ONLY guard running ONLY the 4 subagent stages + fleet-gated-leg tolerance skips. Targeted run findings:
  - subagent_dir: resume→entry open→seeded directory (20 shown/25 total, · 25 head)→paging ALL GREEN; fleet legs (Running/Completed badge flips, rebuild completed=25) blocked: `available=false, fleet=null` — pi-subagents 0.70.1's RPC NEVER ANSWERS under SDK 0.86.1. PROOF of environment (not diff): `npm run smoke:subagents070` (untouched ticket-111 probe, zero PiCode UI) fails at `rpc ping: budget of 15000ms elapsed`; a bare in-process SDK session (observer only) also gets no RPC reply while pi-subagents IS loaded as an extension (extensionsResult lists npm:pi-subagents). Renderer correctly refuses the fold when available=false (by design). Ticket-90 stage is in the batch's documented flaky family — 留档不追.
  - subagent_chat: chat open (row click → task-named tab + child transcript) ✓, ×-close/reopen ✓ (after a REAL selector fix: fixed tab has no ×, so index-aligned close lookup broke — resolved the chat tab's OWN ×), readonly ✓, lost-run error path ✓; fleet-gated: running badge, live tail (follow registers only for live rows), steer composer, close-alive badge.
  - subagent_stop: badge zero ✓, badge zero again ✓, injected-foreground stop (popover→abort dispatch) ✓; fleet-gated: badge=2, stop RPC receipts, Stopping overlay.
  - **subagent_mutex_136 (new stage): ALL 9 LEGS GREEN** — entry→fixed tab, both swap directions inherit widths (560/640), manual collapse never opens the other pane, widths remembered on reopen.
  - TARGETED RUN EXIT=0 with the temp tolerances; every skip logged `SKIP_fleet_unavailable` with the available=false evidence.
- 2026-09-22 13:28 Visual harnesses (inject contract events renderer-side with available:true — verifies the full live chain my smoke couldn't):
  - visual:subagents → s9-1-directory/s9-2-settled/s9-3-expanded/s9-4-empty all captured (opening redirected to entry click).
  - visual:subagent-chat → first run FAILED at "closing the error tab never left the live conversation tab intact" — SAME ×-index misalignment in the harness (fixed tab has no ×). Fixed (body-index → strip-tab → own ×): ALL frames captured (s99-1-chat/s99-2-live/s99-3-receipt/s99-5-error/s99-4-readonly/s101-1-stop-confirm/s101-2-stopping/s101-3-badge). Frames in .scratch/visual/ (tracked, updated).
  - ⚠️ SERIALIZATION VIOLATION: wt-135's electron smoke was running (started 13:27) during my last visual:subagent-chat run and smoke:layout — I did not re-check ps before those two runs. Both passed (isolated userData, no dev server), but the rule was broken. No further electron runs until wt-135 finishes; ps re-check before every run from now on.
  - smoke:layout: green (sidebar 460 persisted, panel 560 persisted, resets ok).
- 2026-09-22 13:30 Final state assembled: ALL temp patches reverted (T136 guard, count lengthening, fleet tolerance skips, rt1 diag, created hoist); KEPT: ticket-99 ×-close selector fix + visual-subagents-chat close fix (real adaptations to the fixed-tab-no-× semantics). vitest 2175/2175 green, typecheck 0 errors on final state. Ticket file copied to worktree .scratch/picode-1-8/issues/.
- Remaining: (a) wait wt-135 electron finish → full electron smoke on final state (expect: everything green up to subagent_dir's fleet legs, which fail on the environment's pi-subagents RPC breakage — documented); (b) self-review (Standards: Fowler, no CJK/TODO in code; Spec: acceptance item-by-item); (c) commit branch + ticket Status→ready-for-human + Comments tip sha.
- 2026-09-22 13:55 DONE. Branch t136-subagent-sidebar: delivery commit 543b57d + docs commit cddf691 (ticket Status→ready-for-human + delivery notes). Post-revert state re-verified: vitest 2175/2175, typecheck 0, build ok. Selector scoping kept as permanent adaptations (two panes share .panel-tab* classes): 90/99/101 subagent stages scoped to .subagent-panel; panel_tabs/86/88 TAB_COUNT/ACTIVE/close probes scoped to .side-panel (panel_tabs had genuinely failed on the shared-class ambiguity before scoping — re-run green); ticket-99 ×-close + visual-subagents-chat close fixed to resolve the tab's own × (fixed tab has none). Ticket file committed with branch. Root .scratch progress file untracked (never git add) ✓.
