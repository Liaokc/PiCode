# 03: 聊天主线程全量

**What to build:** 会话转录完整产品化：markdown 渲染与代码高亮；工具卡片（名称、参数、实时状态、展开/收起、终态 done/error）；思考过程折叠行（含耗时）；"Working · Ns" 进度行；单条消息操作行（copy 等）与时间戳；错误在 UI 各层的清晰上浮。视觉对齐截图 01/04 的信息密度与层级。

**Blocked by:** 02 Host 活体。

**Status:** resolved

- [x] 每类契约事件在 chat reducer 有表驱动测试（含错误路径），Seam-1 无回归口
- [x] 含工具调用的一轮对话视觉密度与截图 01 对照通过（卡片、折叠、状态色）
- [x] thinking 折叠行显示持续秒数，展开平铺推理文本
- [x] 复制等操作真实生效；失败工具结果以明确错误样式呈现

## Comments

- 2026-08-28: implemented on `t03-chat-thread-full`, head `13f545b` (5 commits, twice rebased as main advanced: t10 landed mid-ticket, then t06 — icons/App deduped against t06's additions, single `ChevronDownIcon`, my `XIcon` folded into t06's identical `CloseIcon`).
- Contract additions are additive-only (ticket-02 events untouched): `thinking_delta`, `thinking_end` (host-measured `durationMs`), `tool_start` (id/name/args), `tool_update`, `tool_end` (final output replaces partials, `isError`). Chat state is now a flat entry list — user entries, assistant entries with ordered thinking/text parts, top-level tool cards keyed by toolCallId — folded by a pure reducer with defensive boundaries (delta without message_start, thinking cut short, tool never finished → error card).
- Renderer: react-markdown 10 + remark-gfm + rehype-highlight (new deps); collapsible Thinking row (Thinking… ticks / Thought · Ns frozen), tool cards with live spinner→done ✓/error ✗, expandable Arguments/Output with red error styling, "Working · Ns" line after the run's user message, per-message Copy (real clipboard) + timestamp, markdown with highlighted code and inline-code chips. View-side clocks only (`useElapsedSeconds`) — the reducer stays time-free.
- Verified: `typecheck`/`lint`/`test` green (147 tests; reducer coverage is table-driven per event type incl. error paths + purity freeze). `npm run smoke:host` PASS — real SDK round 2 now drives a real bash tool call (`tool_start`/`tool_update`×2/`tool_end`, `thinking_end` durationMs present). `npm run smoke:electron` PASS (stream→abort→second turn→DOM→SIGKILL→rebuild→clean exit).
- Visual evidence committed at `.scratch/visual/` (1-midrun / 2-settled / 3-expanded), generated by `npm run visual:transcript` (`PICODE_VISUAL=1`: injects a realistic contract stream, captures PNGs, asserts DOM signature each shot — 8/8 deterministic). Structure and density match screenshot 01 (user bubble → Working·Ns → thinking row → text → tool card) and 04 (markdown, action row + timestamp).
- Found & fixed during review: a renderer mount race — the harness could emit `session_created` before React attached the Seam-1 subscription (lost session). The renderer now stamps `data-chat-subscribed` when the subscription is live and the harness waits for it. Also extracted the shared `useElapsedSeconds` ticker (review finding).
- Human pass remaining: eyeball `.scratch/visual/*.png` against `.scratch/reference/screenshots` 截屏 18.54.03 / 18.54.27, and optionally `npm run dev` with a real conversation; copy-click and expand/collapse are mouse interactions not covered by the smokes.
- Merge from the root worktree: `cd ~/PiCode && git merge --no-ff t03-chat-thread-full`.
- 2026-08-28 (merge session): **resolved** — merged into main as `1a8891f`。验收口径：操作者人工验收通过（对照截图 18.54.03/18.54.27 的排版清单 + 交互项）；rebase 到含 t04 的 main 时解了两批功能的接线冲突（host wireSessionEvents、App/ChatView 装配、icons 并集、smoke 双覆盖 union、history_loaded 移植到 entries 模型），解后 typecheck/lint/244 vitest 全绿，并重跑真机 contract smoke：Round A abort+工具轮与 Round B resume/rename/tree/fork 全链 SMOKE PASS。worktree 与分支已清理。
