# 07: 文件预览标签

**What to build:** Side Panel 的 File Preview 标签：markdown 渲染与代码语法高亮、面包屑路径导航；消息流中的文件改动卡片与 Review 条目可 deep-link 直达对应文件预览（对照截图 04/08 右栏形态）。

**Blocked by:** 06 面板容器 + Review 标签。

**Status:** ready-for-human

- [x] md 与源码文件的预览呈现对照截图 08 通过（标题层级、行内代码、表格、高亮主题）
- [x] 从文件改动卡与 Review 文件树一键直达预览，面包屑可逐级回退
- [x] 大文件打开不卡 UI（超长文件策略明确）

## Comments

- 2026-08-28 (worktree wt-07-file-preview): implemented on `t07-file-preview` as **`7a843bc`** — do not merge from this session; operator runs `git merge --no-ff t07-file-preview` from the root worktree.
- Seams: `src/shared/preview/` (policy + pure tab reducer + typed `PreviewResult`), `src/main/preview/read.ts` (fs reader behind the additive `preview:load` IPC channel), `PreviewTab`/`CodeView`/`PreviewLinkChip` in the renderer. Preview tab is deep-link-only (not in the screenshot-03 picker, which stays Review/Terminal); open panel tabs stay mounted so preview navigation survives tab flips.
- Large-file policy (explicit, unit-tested): 2 MB read cap → typed `too-large` failure · markdown over 256 KB renders as source instead of being parsed · source view renders 1,000-line windows with "Show N more lines", hard-capped at 20k lines · directory listings capped at 500 entries.
- Deep-links: file-arg tool cards (read/write/edit/ls) carry an Open chip; Review file-tree rows carry a hover chip; both resolve relative paths against the session cwd and open/activate the Preview tab. Breadcrumb directory levels and the listing view give level-by-level fallback (click any crumb → directory listing → pick a file).
- Visual evidence regenerated by `npm run visual:transcript` (deterministic DOM-signature shots): `.scratch/visual/4-preview-markdown.png` (markdown + Open chip), `5-preview-source.png` (highlighted source + gutter), `6-preview-directory.png` (crumb fallback listing), `7-review-deeplink.png` (Review tree → preview). Shots 1–3 (ticket 03) refreshed in the same run.
- Verification: typecheck + eslint clean, 277/277 vitest (33 new across policy/view-model/reader), `smoke:electron` full contract smoke PASS after wiring.
- Human pass remaining: eyeball `.scratch/visual/4..7` against `.scratch/reference/screenshots` 截屏 18.54.27 / 18.57.01 (标题层级、行内代码、表格、高亮主题、面包屑形态), then optionally `npm run dev` — click Open on a write/edit card and a Review tree row, walk breadcrumbs back level by level, and open a multi-MB file to confirm the too-large notice.
- 2026-08-28 (worktree, operator feedback round 1): addressed on `t07-file-preview` as **`083213c`**. (1) Source view now soft-wraps by default with a toolbar 换行/截断 toggle (`.preview-wrap-toggle`, pressed state visible; preference survives file loads; reducer-tested); evidence shots `5-preview-source.png` (wrapped) + `5b-preview-truncated.png` (truncated). (2) Panel width drag freed: clamp 280–1200px (was 320–760), 11px hit area, always-visible grip line on the panel edge, double-click reset unchanged; clamp pinned by test.
